<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/media_item_background"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/albumArt"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/album_art_alt"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@android:color/transparent" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/divider"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_end="@dimen/audio_controls_divider_size" />

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/audio_player_overlay_height"
        android:layout_marginTop="@dimen/audio_player_overlay_margin"
        android:background="@drawable/media_overlay_background"
        app:layout_constraintBottom_toBottomOf="@+id/albumArt"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/divider" />

    <ImageButton
        android:id="@+id/mediaButton"
        android:layout_width="@dimen/exo_media_button_width"
        android:layout_height="@dimen/exo_media_button_height"
        android:background="?attr/selectableItemBackground"
        android:scaleType="centerInside"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/divider"
        app:srcCompat="@drawable/ic_play_arrow_black_24dp"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/audio_player_title_margin_start"
        android:layout_marginTop="@dimen/audio_player_title_margin_top"
        android:layout_marginEnd="@dimen/audio_player_title_margin_end"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="@style/TextAppearance.PodPlay.Title"
        app:layout_constraintLeft_toRightOf="@id/mediaButton"
        app:layout_constraintRight_toLeftOf="@id/position"
        app:layout_constraintTop_toBottomOf="@id/divider"
        tools:text="Podcast Episode Title" />

    <TextView
        android:id="@+id/subtitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/audio_player_subtitle_margin"
        android:layout_marginEnd="@dimen/audio_player_subtitle_margin"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="@style/TextAppearance.PodPlay.Subtitle"
        app:layout_constraintLeft_toRightOf="@id/mediaButton"
        app:layout_constraintRight_toLeftOf="@id/position"
        app:layout_constraintTop_toBottomOf="@+id/title"
        tools:text="Channel Name" />

    <TextView
        android:id="@+id/position"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/audio_player_position_margin_start"
        android:layout_marginTop="@dimen/audio_player_position_margin_top"
        android:layout_marginEnd="@dimen/audio_player_position_margin_end"
        android:maxLines="1"
        android:textAppearance="@style/TextAppearance.PodPlay.Title"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/divider"
        tools:text="00:00" />

    <TextView
        android:id="@+id/duration"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/audio_player_duration_margin"
        android:layout_marginEnd="@dimen/audio_player_duration_margin"
        android:maxLines="1"
        android:textAppearance="@style/TextAppearance.PodPlay.Subtitle"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/position"
        tools:text="00:00" />

</androidx.constraintlayout.widget.ConstraintLayout>
